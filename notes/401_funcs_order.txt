Очередность функций
Мы можем явно определить функциию, которую компилятор должен ожидать
просто обявив её вначале не реализую
float do_some(int some);
и все это можно подключить в заголовочный файл, нопример
stdio.h тоже заголовояный файл
Если мы пишем програму
totaller.c, то заголовочный ыайл будет totaller.h, функйию main() в заголовочный файл добавлять ненужно
* подключать #include "totaller.h" !!! важно в кавычках
219
